import React from 'react'
import Button from '../../UI/Button'

export default function AddEventDialogActions({onSubmit}) {
  return (
    <Button onClick={onSubmit}>
       
        Submit Event
      
    </Button>
  )
}
